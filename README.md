# omenaai

Turborepo workspace with a separated frontend and backend deployment target.

## Apps

- `apps/web`: Next.js frontend
- `apps/api`: Hono backend

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm lint
pnpm typecheck
```
